# Example environment configuration for GitHub Pages deployment
# Copy this file to .env.local and configure for your environment

# Base URL (automatically set by GitHub Pages)
# NUXT_APP_BASE_URL=/your-repo-name

# API Configuration (for production you might want to use external APIs)
# NUXT_PUBLIC_API_BASE=https://your-api-domain.com/api

# JWT Secret (required for server-side auth)
# JWT_SECRET=your-very-secure-secret-key-here

# Database Configuration (currently using Neon/PostgreSQL connection string)
# DATABASE_URL=postgresql://username:password@host:port/database?sslmode=require

# Application Settings
NUXT_PUBLIC_APP_NAME=InMyCity
